language: minimal

services:
  - docker

before_install:
  - docker pull hadolint/hadolint

stages:
  - test
  - deploy

jobs:
  include:
    - stage: test
      script: make ci
    - stage: deploy
      script: make push-image
      if: branch = master
